;[Runtime]
runtime_log=manticore_xrt.log
; print to console, alternatives are null/syslog/filename
;verbosity=0; max value is 3 default is 0

[Emulation]
debug_mode=gui
;; see the emulation log nin emulation_debug.log
print_infos_in_console=false
; alternative is batch or off
; aliveness_message_interval=60 ; print a liveness message every minute


; For the first run, run simulation in GUI mode. Add signals that you want to
; add. Copy the commands from the Tcl console and save into a Tcl script. For
; the next run, pass the Tcl script in batch mode.

user_pre_sim_script=loadwaves.tcl
